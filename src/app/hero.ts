/**
 * Notice that class Hero has "export" keyword.
 * "export" allows this class to be imported in some other component or class.
 * for example, this Hero class is imported in mock-heroes.ts
 */

export class Hero{
    id : number;
    name : string;
}